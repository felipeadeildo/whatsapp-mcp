services:
  wuzapi:
    build:
      context: ./wuzapi
      dockerfile: Dockerfile
    env_file: .env
    volumes:
      - ./dbdata:/app/dbdata
    ports:
      - '${WUZAPI_PORT:-}:8080'
    environment:
      - WEBHOOK_FORMAT=json
