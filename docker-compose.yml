services:
  wuzapi:
    build:
      context: ./wuzapi
      dockerfile: Dockerfile
    volumes:
      - ./dbdata:/app/dbdata
    ports:
      - "${WUZAPI_PORT:-}:8080"
    environment:
      - WUZAPI_ADMIN_TOKEN=H4Zbhw72PBKdTIgS
      - TZ=America/Sao_Paulo
      - WEBHOOK_FORMAT=json
      - SESSION_DEVICE_NAME=WhatsApp MCP